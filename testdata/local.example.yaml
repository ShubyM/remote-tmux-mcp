hosts:
  local:
    local: true
    remote_binary: /tmp/tmux-mcp-test/remote-tmux-mcp
    default_session: agent-test
    remote_state_dir: /tmp/tmux-mcp-test/state
    tmux_socket_name: tmux-test
    max_output_bytes: 65536

defaults:
  keep_open: true
  output_tail_lines: 200
